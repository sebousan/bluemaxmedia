{{- $fields := slice 
  "video_platform" 
  "video_id"
  "is_shorts" -}}
{{- $fields = partial "admin/func/GetFields.html" (dict "field" . "fields" $fields) -}}

{{- $args := dict 
  "label" (i18n "admin.fields.video.embed.label")
  "name" "video_embed_5"
  "required" false
  "fields" $fields
  -}}

{{ partial "admin/widgets/object.js" $args }}
